rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Match files in a user-specific folder
    match /users/{userId}/{allPaths=**} {
      // Allow read and write access only to the user who owns the folder
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
